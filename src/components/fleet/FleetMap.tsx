import React from "react";
export default function FleetMap({ locations }: { locations?: any[] }) {
  return <div className="h-64 bg-muted rounded-lg flex items-center justify-center text-muted-foreground">Mapa da frota (stub)</div>;
}
