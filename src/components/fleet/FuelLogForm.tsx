import React from "react";
export default function FuelLogForm({ employeeId, onClose }: { employeeId: string; onClose?: () => void }) {
  return <div className="text-muted-foreground">Formulário de combustível (stub)</div>;
}
