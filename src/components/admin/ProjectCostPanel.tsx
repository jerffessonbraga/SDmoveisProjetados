import React from "react";
const ProjectCostPanel: React.FC<{ projectId?: string; projectName?: string; totalValue?: number }> = () => <div>Custos do Projeto — stub</div>;
export default ProjectCostPanel;
